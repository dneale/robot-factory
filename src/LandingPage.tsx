import * as React from 'react';
import {Button, Container} from 'semantic-ui-react';

const LandingPage = () => (
  <Container text={true}>
    <Button>Start QA</Button>
  </Container>
);

export default LandingPage;
